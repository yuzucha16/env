[user]
	name = user
	email = user@example.com
[core]
    editor = vim
	eol = lf
    autocrlf = false
    symlinks = true
	longpaths = true
	excludesfile = ~/.gitigonore_global
	whitespace = cr-at-eol,tab-in-indent,trail,space-before-tab
[apply]
	whitespace = fix
[pull]
    ff = only
    rebase = false
[color]
    ui = true
[mergetool]
    keepBackup = false
[filter "lfs"]
    required = true
    clean = git-lfs clean -- %f
    smudge = git-lfs smudge -- %f
    process = git-lfs filter-process
#[http]
#	sslbackend = schannel
#	sslVerify = true
